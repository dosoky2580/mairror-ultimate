name: mairror_ultimate
description: تطبيق المرايا المتكامل - نسخة الحوار الثنائي
version: 1.0.0+1
environment: { sdk: '>=3.2.0 <4.0.0' }
dependencies:
  flutter: { sdk: flutter }
  provider: ^6.1.1
  translator: ^0.1.7
  flutter_tts: ^4.2.2
  share_plus: ^9.0.0
  speech_to_text: ^6.6.2
dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^3.0.0
flutter: { uses-material-design: true }
