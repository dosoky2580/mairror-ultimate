name: mairror_ultimate
description: Mirror Ultimate - Security & AI Edition
version: 1.0.0+43
environment: { sdk: '>=3.2.0 <4.0.0' }

dependencies:
  flutter: { sdk: flutter }
  provider: ^6.1.1
  # الترجمة والإنترنت (الميثاق)
  translator: ^1.0.4+1
  http: ^1.1.0
  # الحماية والتخزين
  flutter_secure_storage: ^9.0.0
  encrypt: ^5.0.3
  firebase_remote_config: ^4.3.8
  # الكاميرا والذكاء الاصطناعي
  camera: ^0.10.5+5
  google_ml_kit: ^0.16.0
  # الألعاب والرسوم
  flame: ^1.8.0
  chess: ^0.11.0
  # أخرى
  path_provider: ^2.1.1
  share_plus: ^7.2.1
  url_launcher: ^6.2.1
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^3.0.0

flutter: { uses-material-design: true }
