name: mairror_ultimate
description: نسخة الحوار والترجمة الشاملة - ميرور
version: 1.0.0+1
environment: { sdk: '>=3.2.0 <4.0.0' }
dependencies:
  flutter: { sdk: flutter }
  provider: ^6.1.1
  translator: ^0.1.7
  flutter_tts: ^4.2.2
  share_plus: ^10.1.4
  speech_to_text: ^7.0.0
  http: ^1.1.0
  permission_handler: ^11.0.1
dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^3.0.0
flutter: { uses-material-design: true }
