name: mairror_ultimate
description: Mirror Ultimate - Version Compatibility Fix
version: 1.0.0+48
environment: { sdk: '>=3.2.0 <4.0.0' }

dependencies:
  flutter: { sdk: flutter }
  provider: ^6.1.1
  translator: ^1.0.4+1
  http: ^1.1.0
  flutter_tts: ^3.8.3
  flutter_secure_storage: ^9.0.0
  encrypt: ^5.0.3
  firebase_remote_config: ^4.3.8
  # التحديث للنسخ المتوافقة حسب اقتراح جيت هب
  google_mlkit_text_recognition: ^0.13.0
  google_mlkit_translation: ^0.10.0
  google_mlkit_language_id: ^0.10.0
  camera: ^0.10.5+5
  flame: ^1.8.0
  chess: ^0.8.1
  path_provider: ^2.1.1
  share_plus: ^7.2.1
  url_launcher: ^6.2.1
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^3.0.0

flutter: { uses-material-design: true }
